extends layout

block content
  .container
    h2 회원가입
    form(action="/auth/signup" method="POST")
      label(for="username") 아이디:
      input(type="text" name="username" id="username" placeholder="아이디를 입력하세요" required)

      label(for="password") 비밀번호:
      input(type="password" name="password" id="password" placeholder="비밀번호를 입력하세요" required)

      label(for="name") 이름:
      input(type="text" name="name" id="name" placeholder="이름을 입력하세요" required)

      label(for="organization") 소속:
      input(type="text" name="organization" id="organization" placeholder="소속을 입력하세요")

      button(type="submit") 회원가입
    p
      a(href="/") 로그인 화면으로 돌아가기
